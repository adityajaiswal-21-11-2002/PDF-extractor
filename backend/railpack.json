{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "steps": {
    "install": {
      "commands": ["pip install --upgrade pip setuptools", "pip install -r requirements.txt"]
    }
  },
  "deploy": {
    "startCommand": "bash start.sh"
  }
}
